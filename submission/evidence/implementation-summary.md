# MindCraft implementation progress

Author: root, 2026-09-09. Stage: Code Generation IN_PROGRESS. Approved design: AP-GUI-DESIGN-001. No product gate is approved by this progress record.

## Markdown foundation schema checkpoint

2026-09-09T09:42:08+09:00 addition: `document-fence-block.ts` matches one active fence within a caller-selected context and emits raw source ranges without retaining code body text. Physical line continuity is separate from projected content start, verified with quote oracle cases and real stored-source integration. Twelve combined tests pass and independent review accepts the corrected dependency scope. `aidlc-docs/reviews/document-fence-block-review.md` preserves the projection failure and hashes. Actual container dispatch, disk frames, body decoding and complete parser/GUI remain open.

2026-09-09T09:35:27+09:00 addition: `document-source-cursor.ts` and `document-fence.ts` connect verified scalar reads to isolated fence-line recognition without whole-line accumulation. Root 11 tests pass, including actual stored old-prefix ledger/cursor/fence integration and small installed-parser comparisons. Independent hashes and review scope are in `aidlc-docs/reviews/document-cursor-fence-review.md`. Full context-aware block grammar, semantic publication and GUI integration remain incomplete.

2026-09-09T09:30:13+09:00 addition: `src/core/document-source-ledger.ts` consumes verified source pages into session-private disk-backed line/page offsets without storing body text. Root 12 tests pass, including real Store prefix binding, 1.25MiB single line, 3,001-line paging, corrupted metadata, rollback and active-read close. Final source review and preserved failures are in `aidlc-docs/reviews/document-source-ledger-review.md`. This is source metadata groundwork; semantic Markdown parsing, durable catalog and renderer integration remain open.

2026-09-09T09:21:10+09:00 addition: `src/core/document-source.ts` provides fixed-version, bounded verified source traversal for private parser staging. Six root tests and tsc pass, including actual stored output pinning and creation-time descriptor mutation. Independent review confirms the corrected pin behavior; original failure and hash evidence are in `aidlc-docs/reviews/document-source-review.md`. Disk source-ledger/index publication and parser/GUI integration remain next work.

Under AP-MARKDOWN-FOUNDATION-001, `src/shared/document-contracts.ts` now supplies strict DTO and request schemas for derived document state/pages/relations/attributes/anchors, Unicode-safe text and a 64KiB successful-Reply JSON budget. Root-authored `tests/document-contracts.test.ts` passes 11 checks after two independent review counterexamples were reproduced and corrected; tsc passes. Source hash and original failures are preserved in `aidlc-docs/reviews/document-schema-focused-review.md`. No new document command is registered yet. Version-bound source ledger, disk parser/index, endpoint integration and actual formatted long-document GUI remain unimplemented by this batch; earlier output paging behavior remains current.

## Current checkpoint — joint wire v2 and process tracking correction

2026-09-09, root. This current section supersedes older current-tense progress statements below; those entries remain historical evidence.

Actual Core, desktop bridge, GUI and CLI/TUI now use strict protocol 2, body-free Run summaries/details and explicit bounded output pages. Atomic coordinator publication is connected to production Events. Transport pages coalesce storage chunks into scalar-complete windows up to 64KiB. A selected version stays pinned for full copy/save/capture; native retry streams the originally selected version after fresh approval. Capture applies existing redaction over the complete verified prefix and distinguishes source/body counts and hashes.

| Requirement/approved plan scope | Actual source | Current evidence |
| --- | --- | --- |
| Steps 1/2/6; body-paging §3; C-7 | shared contracts/validation, Core Events/output-publication | Root wire/events/review plus terminal combined 48 PASS; independent wire-specific 22 PASS. Legacy reset and v1 rejection, durable owner/sequence checks and exact receipt binding included. |
| Steps 2/3; body-paging §§4–6/8 | Core service/output-requests/Store | Actual Core 4 PASS: metadata-only views, pinned prefix actions, restart/fresh approval/bounded save, many-small-chunk forward/reverse pages and cancellation. Independent coalescing case 1 PASS. Lazy-output Store 3 PASS, independently repeated. |
| Step 3; capture redaction/provenance | Core knowledge.capture | Actual Core independent counterexample changed from 1 FAIL to 1 PASS; root also passed it. Raw source and redacted body provenance checked separately. |
| Step 7; terminal status/read/cancel | terminal cli/output | Root terminal+review 24 PASS; independent five lifecycle counterexamples PASS; page-writer 4 PASS. Earlier host/resize/shutdown/terminal batch 55 PASS. Counts overlap and are not summed into a product gate. |
| Steps 6/7; GUI-05/08 | desktop main/host/IPC, renderer Client/OutputViewer | Actual Electron IPC 1 PASS; client 9 author tests PASS; Knowledge/recovery Electron 3 PASS. |
| Step 7; GUI sample/save/copy | actual development build | Sample GUI/save 1 PASS, actual full system clipboard equality and byte-count test 1 PASS. Root inspected real workspace and 1024/200% screenshots. Native IME and complete GUI acceptance remain distinct. |
| Step 8; partial PBT | property-invariants | Root five existing fixed-seed properties PASS with v2 request/event fixtures. Original wider PBT gate remains incomplete. |

The joint development build and typecheck pass. Vite reports a 510.40kB minified renderer chunk warning; no new release is claimed. Current source is newer than preserved build-SOpzFF. GUI tests initially failed because tiny storage chunks became tiny display pages; the bounded transport-window correction fixed the actual journeys without weakening formatting assertions. Failed attempts and repeated-issue reviews are preserved in `../../../reviews/wire-v2-integration-focused-review.md`.

Remaining requirements include full version-bound disk-backed Markdown parsing, paged approval diffs, other context/Knowledge/runtime capacity paths, strict command confinement, actual provider evidence and the full original acceptance matrix. Whole clipboard and current capture/diff preparation still materialize complete strings; they are not claimed bounded-memory. Formal Build and Test has not started as a completion stage. Process tracking omissions and corrections are recorded in `../../../reviews/aidlc-process-audit-20260909.md`; current checked substeps are in `../../plans/mindcraft-code-generation-progress.md`.

## Implemented and exercised

- Shared C-7 DTOs, all command payload/result schemas and event validation.
- Core SQLite/body storage, mode-scoped tasks/runs/events, context confirmation, single-use approvals, effects, knowledge capture/promotion and explicit offline samples.
- Pi Agent runtime with custom tools and guarded provider transport. Actual provider connections have not been made.
- Windows native file handles, writer locking, protected credential helper, Electron vault adapter and host composition. Arbitrary commands remain fail-closed because strict LPAC filesystem confinement failed in the spike.
- React presentation and terminal adapters are integrated. Actual Electron sample/context/output/approval/disk-save/setup journey passes; home, workspace, approval and setup PNGs were captured from the running application.

Actual `node --import tsx --test tests/core.test.ts`: PASS, one real filesystem/SQLite offline journey reading the three bundled Korean sources, confirming context, deriving output, approving and verifying saved bytes, capturing and promoting Knowledge. This does not prove provider, scale, recovery or GUI gates.

Actual `node scripts/build-native.mjs` and native suite: PASS for held-handle competing writes/rename rejection, new and existing target publication, stale identity, one-use/approved hash checking, independent process writer exclusion/release, and synthetic Credential Manager roundtrip/removal. DPAPI and packaged terminal evidence pending.

## Independent native review NATIVE-REVIEW-001

Reviewer: runtime_implementation (read-only). Findings and disposition:

1. Protected-file hardlinks bypassed path exclusion: initial correction rejects multi-link file handles before any content read. Actual malicious hardlink regression now rejects the read; no full security approval is implied.
2. File permits omitted approved content hashes: Core now binds immutable before/after constraints; native adapter binds them to the issued handle and validates mutation calls. Mutation negative assertion passes.
3. Existing-target replacement preserves old bytes but lacks durable effect-linked backup recovery descriptor: OPEN. Current non-atomic rename publication must not be claimed fully crash recoverable. Implement descriptor plus inspection/recovery before G-06 approval.
4. Volume/share-root directory reads need explicit native handling: OPEN; ordinary directory workspace journey passes.

## Required remaining work

Complete GUI/CLI/TUI rendering and journeys; native negative tests and recovery descriptors; strict command confinement; per-record corruption quarantine and relational ownership enforcement; true streaming capacity; model metadata/context partition contracts and full split execution; local wire protocol/security tests; actual provider and clean Windows evidence; all G-01–12 acceptance; EXE/source ZIP/checksums/README/screenshots and independent final review. Preserve AC-01–16 and the 17 stories. No fixture or local protocol test substitutes for real provider evidence.

## Desktop integration evidence
`node scripts/build.mjs`: PASS (TypeScript, native helper, Vite production, main/preload bundles). `node --import tsx --test tests/gui.electron.ts`: PASS, one actual Electron journey, renderer lacks require/process, three sources confirmed, output completed, exact file approved and saved, setup screen opened; no page errors. ESM main no longer awaits app readiness at module evaluation (Playwright startup dependency deadlock corrected). GUI test selector corrected from nonexistent .document to actual .markdown; no product assertion was weakened. Screenshot content viewport initially 1424×861 from outer 1440×900 Windows frame; subsequent capture explicitly sets content dimensions. All remaining gate obligations still open.

## 2026-09-09 integration continuation
- 22/22 deterministic tests PASS across actual core/native, per-record corruption isolation, event recovery, and loopback transport suites. No skipped tests in this command.
- Actual Electron GUI journey PASS including exact content sizes1440×900 and1024×700, sourceconfirmation/save, malformed request rejection, and200% reflow assertion. Native capturePage is used for200% screenshot because the Chromium automation screenshot cropped to CSS viewport pixels.
- Copy implementation now awaits the browser clipboard promise and authorizes only sanitized writes from the exact trusted main frame; reads and other permissions remain denied. Separate tests/clipboard.electron.ts FAILS its system roundtrip precondition: Electron write resolves but readback is empty, and independent Windows Get-Clipboard fails. Actual product copy is UNVERIFIED; this failure remains visible and is not counted in22 passing tests or a completed G-11.
- Five adversarial record/blob corruption tests PASS. Original bytes/rows remain preserved; valid histories remain usable. Database-wide corruption still fails closed; full relation/migration/capacity proof remains pending.
- Additional isolation spikes tried an extra staging-only restricting SID with both .NET and no-CRT native/cmd executables. Token restrictions were verified but processes exited0xC0000022 before entry; baselineLPAC still leaks ARAP-readable canary. Productcommand isolation remains NOT_PASS. Results stay in project-local ignored test tree; no globalpolicy/ACL/BFS/privilege changes.
- Next implementation priorities: recoverable effect-linked native publication journal, typed model budgets/context partitioning, relational persistence/large-data streaming, remaining CLI/TUI/IPC/native/PBT acceptance, portable package and clean Windows/provider evidence.

## Publication recovery, partition runtime and package integrity continuation

- Native journals now persist scoped approval/target/before/after/stage/backup descriptors across prepared, original-preserved and published boundaries. Root reran nine fault/tampering/junction tests plus the two existing native tests: 11/11 PASS. An additional real Windows absence-observation test subsequently passed with both existing native tests (3/3); missing leaves and directories return metadata-only handles after verification, with no content reads. Commits `498fb2b`, `74de8d7`.
- Core recovery attaches historical journal facts, verifies actual target bytes/absence with fresh external consent, and preserves old effects while creating new retry proposals. Independent review found missing-target consent and early-approved retry races; corrections require a completed durable resolution dependency before execution. Root reran recovery/corruption/actual-core regression: 32/32 PASS. These tests combine controlled fault ports with actual filesystem regression; they do not prove network-share or clean-Windows recovery. Commit `4cf658f`.
- Pi runtime now supports exact UTF-8 partitions, individually approved map/reduce/final/history-reduction requests, retained full transcripts and derivation references. Root reran actual Pi/local HTTP partition and guarded transport tests: 17/17 PASS, including empty selected context. No live provider was contacted. Core must still capture candidate source bytes and bind the full planning result into the user-confirmed manifest before this feature is integrated end to end. Commit `8cdec8d`.
- GUI/CLI expose optional model budgets with explicit unset/edit semantics. Product icon is generated from code and portable extraction uses independent launcher directories. Commit `0bb2bfc`.
- Baseline portable EXE generation succeeded after the tool regenerated a failed NSIS cache, but its actual executable failed startup. Root verified nine corrupted ASAR entries caused by native compilation changing dist during packaging. That artifact is INVALID and cannot satisfy G-01/G-02. The focused review records its hash and the required snapshot/integrity/executable tests. New archive verifier detects the known corruption, and two independent archive tampering/entrypoint tests PASS. Fresh snapshot packaging and executable validation remain pending.
- Terminal approval waiting was made responsive and passed nine stream-driven tests; further independent review found same-run approval ambiguity and masked-input EOF cleanup gaps. These are recorded in terminal-approval-focused-review.md and must be corrected before terminal approval or G-12 completion.

All product gates remain subject to their full acceptance scope. No report integration, old repository access, Claude/DDT invocation or existing-harness changes were used.

## Current correction evidence
- Context capture and atomic preview refresh are integrated in commit c02abbe; independent R1 review passed and root ran 18 planning/storage tests. Pi planning no longer depends on an unbound live reread.
- Terminal displayed-approval identity and masked-input cleanup corrections are committed in 364a457; root ran 17 terminal tests successfully.
- Frozen packaging now passes all 16,208 archive body checks and the actual directory GUI sample/save journey. The portable wrapper launches in Korean paths, but the previous preview captured its own EXE; it remains a preview pending rebuilt portable verification.
- Trusted application paths now flow from desktop to Core and native access checks. Root ran 40 tests across host (27), desktop paths (11), actual Core path exclusion (1), and actual native path exclusion (1), all passing. Owned files are skipped before content opening and denied to explicit tools; neighboring user `.exe` text files remain eligible.
- Root initially assigned two agents the same new test filename. The collision was detected from test output; Core coverage was restored under core-application-paths.test.ts, and both distinct suites were run together. No test coverage was intentionally removed.
- Shared host teardown and request identity corrections passed direct focused review. This is implementation evidence, not approval of all product gates. Existing strict confinement, actual provider, clipboard, clean Windows, capacity, and packaged terminal obligations remain open.

## Integrated EXE verification, 2026-09-09 02:36 KST
The rebuilt portable EXE (`29adc88d34731a7c95cdcaf70907109524c563892617a7b13168f4917a000eac`) passes archive/native integrity, developer GUI, packaged directory GUI, and actual renamed portable Korean-folder sample/context/approved-save journeys. The portable test preserves its original three-source assertion. New real packaged CLI/TUI pipe smoke exposes early CLI exit in both distributions; TUI is not reached and remains unverified. This failure is the next terminal implementation investigation. Product gates remain incomplete.

## Streamed storage first batch
Store SHA-256 `d626d69ee8fddd3bde9a3aa3254a7d9fdda1916de52b5d5b360bcccb68f222d6` was independently reviewed by inception_author and directly compared by root. blobStream adds writing/sealed/published intent handling, bounded writes with incremental hashing, cancellation and no-replace publication. Startup integrity verification no longer reads complete bodies into memory. Producer/write/sync/journal/link/unlink/collision failures preserve evidence without returning success. Root ran combined storage/context/corruption/host/terminal/actual-Electron descriptor regression: 93/93 PASS. Existing body() and source/context/Knowledge/history/runtime interfaces still materialize complete payloads; full G-09 and SMB publication capability remain unverified and required.

## Integrated artifact and GUI correction evidence
- Production source build passed after terminal, streamed storage and Knowledge/recovery corrections. Root's final relevant host/terminal/actual-Electron shutdown regression53/53 passed; preceding combined storage/context/corruption regression93/93 passed. These commands overlap and are not summed as distinct coverage.
- Three actual Electron tests with explicitly controlled IPC passed for Knowledge filtering/pagination/provenance/decisions, recovery navigation/defer/resolution, and active-effect→uncertain-result transitions. Their controlled-* screenshots cover1440/1024; they do not prove real native effect or provider behavior.
- Current preview EXE `release/MindCraft-0.1.0-windows-x64.exe`:118,809,781 bytes, SHA-256 `b16ad2ec7dc87d99cad0604361a4b47e930c683bebf8dd387bec8a513cc85cd3`. All16,208 ASAR bodies and native snapshot verified. Actual directory and portable CLI→TUI Korean pipe/persistence/exit smoke PASS; actual portable GUI sample/context/save PASS.
- Source ZIP `release/MindCraft-0.1.0-source.zip`:1,348,848 bytes, SHA-256 `029584b816336f884cf02c2655b8d29d0d69c26881bd8f097d13e279872c4eff`, sourcecommit `0eecd34b5e988d2c8d6f70a1ea9e1268f4610d03`. Root decompressed all182 entries and compared their Git blob hashes with the committed product tree; all matched. Initial export from a nested cwd incorrectly produced an empty archive; running archive at repository root fixed it. Source differs from compiled18ad6bf only by the export helper cwd correction. `release/SHA256SUMS.txt` records both artifact hashes.
- Gate status remains Code Generation IN_PROGRESS. Interactive TTY/ConPTY, same-workspace GUI→CLI→TUI+Busy, actual provider/DPAPI/clipboard, strict command confinement, full cross-module streaming and100GiB+100GiB+20GiB capacity, PBT seeds/shrinking, complete IPC/GUI negative journeys and clean Windows evidence are still required. No gate is approved by these preview artifacts.

## Same-workspace presenter continuity
Root ran tests/presenter-parity.packaged.ts against the actual current directory EXE: PASS. One isolated workspace is opened in GUI, rejects a second CLI writer with the workspaceBusy message, completes the real three-source sample, captures and promotes Knowledge, then closes. CLI and TUI inspect the exact persisted run/episode IDs, full output and Knowledge provenance; CLI adds a task, and a reopened GUI shows that task and the promoted Knowledge. Actual SQLite records are read only for identity verification. The first assertion matched both sidebar and history labels; it was scoped to the history task row without changing the product or expected content. TypeScript passed. This extends G-12 evidence; interactive TTY/ConPTY keys/resize/masking are still separately pending, and no overall completion is claimed.

## Property invariants and presenter assertion review
Independent review found that repeated terminal commands could match earlier accumulated output. Root changed each command assertion to search only output received after that command was sent, then reran the actual packaged GUI→CLI→TUI→GUI test: PASS (24.3 seconds). The prior evidence is strengthened by this run; no production behavior changed.

Root also ran tests/property-invariants.test.ts: five properties, 664 generated cases PASS with fixed seeds and default shrinking. Independent review confirmed real production validation, opaque file/network permission binding, replay/cancellation rejection, SQLite mode isolation, keyset pagination and transaction rollback. This is a partial PBT-02/03/07/08/09 batch. It does not cover all C7 commands, lifecycle state machines, native enforcement, IPC, crash/migration cases or representative capacity, and does not close G-08.

## Existing-console input and next paging contract
After the focused review, Windows terminal input now opens only its existing console when inherited input is a non-TTY character device and output is a real TTY. Valid inherited pipes/files are preserved, newly opened descriptors have explicit ownership, and no other process/console/harness is attached or changed. Root's actual native ConPTY baseline and console-parent masking tests pass (2/2), as do terminal/shutdown/actual-Electron tests including regular-file stdin (27/27). Direct ConPTY remains a preserved failing case with empty capture despite completed writes and a live event loop; actual packaged keys/resize and rebuilding remain pending. See packaged-terminal-focused-review.md for source hashes and evidence limits.

The next body-paging contract was independently reviewed and root compared it with actual fullRun/runPage/renderer accumulation paths. Review identified a selected-version Knowledge capture gap; the final contract binds capture requests and provenance to the selected complete prefix, including V1/V2 concurrency. This is a design dependency for implementation, not implemented paging or capacity evidence. Full material/context/Knowledge/history/runtime streaming and actual 100GiB+100GiB+20GiB verification remain open.

## Actual IPC boundary regression
Root authored and ran tests/ipc-boundary.electron.ts: PASS. The test bundles production installIpc, forwardEvents, strict validators and preload into actual sandboxed Electron windows, with an explicitly controlled Core spy. Valid requests reach Core; malformed envelopes and a same-URL foreign window do not. Main-process navigation intentionally bypasses the product navigation guard and independently proves the IPC exact-URL check. Malformed replies and thrown internal errors produce sanitized, distinct errors; an invalid event is discarded before the subsequent valid event reaches the renderer. Independent read-only review found no false-positive in these assertions. TypeScript passed. The initial fixture used top-level await app.whenReady and timed out during Electron initialization; changing the fixture to an invoked async start function fixed startup without modifying production or weakening assertions. Full product CSP, subframe sender, Core authority and packaged acceptance remain separate obligations; this test does not close G-10.

## Verified body-page primitive and release isolation
Root ran 60 storage/page/context tests successfully after independent review and correction of full-index repeated scans, sticky corruption across owners, and a newer-diagnostic race. The primitive verifies whole/chunk indexes before exposure, authenticates disk-backed temporary page metadata and checks returned bytes; it is not yet called by Store/C7/GUI/CLI. Full body paging integration and capacity proof remain required.

Release tooling now keeps old preview paths unchanged and produces a fresh build directory. Repository releases compile into private output, bind frozen inputs to a compiler receipt, require an unchanged clean source commit, and publish the EXE/source ZIP manifest/checksums together under an atomic complete/ marker. Root's nine fixture/helper tests and independent review pass; actual release execution remains the next check. General source-only build/package does not require Git. ConPTY acceptance uses actual console-parent launches; direct GUI attachment failures are preserved separately as diagnostics after the original requirements and Windows launch assumptions were reviewed. Interactive packaged acceptance remains pending.

## Source-matched release execution and remaining terminal failures

Root completed the release from clean product commit `6cb1b9be5792ebb6f774affcafede43fba11b246`, product tree `3a90ef17763235ad28e555fe108eabb48dfb85d2`, at 2026-09-08T19:11:30.578Z. Artifacts reside in `release/build-CuiH6W`; the old fixed preview paths remain unchanged. The atomic `complete/` receipt proves this artifact set was generated, not that product acceptance is complete.

- Portable EXE: 118,810,968 bytes; SHA-256 `243188ac884c9025a2480bc13a5ce069dc6ba31ad2761f08bb7820af3a65954c`.
- Source ZIP: 1,408,862 bytes; SHA-256 `3a6f464ca9b53705795de5094bc1cd6ce99bc136abad10446288ec9360fe6d31`. Root decompressed all 197 entries and compared actual Git blob hashes with that committed product tree: all matched.
- Frozen package verification checked all 16,208 ASAR files and the native helper against compiler inputs. Actual packaged directory sample/context/approved native save and GUI→CLI→TUI→GUI same-workspace persistence/Busy tests passed (2/2). Test paths now accept an explicit `MINDCRAFT_TEST_PACKAGE_ROOT`, preserving the historical default.
- Actual ConPTY acceptance finished with 2 passes and 2 failures. Native console baseline and console-parent masking passed. Directory Korean CLI commands and TUI F1/F2 worked, but the first 80×24 resize failed the clear-screen-count assertion. Portable console-parent startup exited 1 before the Korean welcome. The preserved evidence directories are `directory-tui-parent-pZ6Ryl` and `portable-cli-parent-75qoyS` under `.test-data/conpty`. Neither failure is waived; diagnosis must distinguish terminal behavior from assumptions about emitted VT sequences.

Code Generation remains IN_PROGRESS. Real provider connections, strict command confinement, clipboard, full streaming consumers/capacity and the remaining GUI/terminal/clean-Windows acceptance still require evidence.

The explicitly selected new portable EXE also passed the actual renamed Korean-folder GUI test (1/1, 35.6 seconds): original deployment folder selection, three source cards, sample execution and approved Korean-named file save. This is current-host evidence, not clean Windows or portable interactive terminal acceptance.

## Legacy output preparation contract review
Root compared Store.chunks/append, fullRun and History result-check behavior with the amended body-paging contract, SHA-256 e6fc43fa6f8d7a8f1b0e8ed4aba7a6859a64817df2dc4c272566e23057d02a6b. Independent review passed the same version. Existing chunks have no exact UTF-16 totals, so a strict metadataPending union replaces fabricated empty counts until explicit cancellable preparation succeeds. Lists/detail metadata do not open bodies. Preparation freezes its accepted prefix, preserves known corruption, separates each waiter's cancellation, and restores unfinished work as idle after restart. The summary resultCheckRequired preserves the existing nonactive uncertain-effect recovery priority. GUI/CLI/TUI and whole-result operations must retain the prepared version even while a newer prefix appears. These are reviewed implementation requirements, not implemented paging or a capacity pass.

## Actual same-window subframe IPC rejection
Root extended the real Electron IPC boundary fixture and reran it successfully (1/1). The fixture explicitly permits frame loading and preload execution in subframes, then proves that a child at the exact trusted URL has the production bridge. Its request receives permissionDenied without increasing Core spy calls; a subsequent main-frame request succeeds and increments the count. Thus the test independently exercises senderFrame identity rather than passing because a child bridge is absent or all IPC is broken. Independent read-only review passed. The relaxed settings exist only in the fixture; product CSP and complete Core authority acceptance remain separate.

## Explicit packaged terminal test target
Root corrected terminal-packaged-smoke.packaged.ts to honor the same MINDCRAFT_TEST_PACKAGE_ROOT selection as the other packaged tests. Previously this one suite silently copied the old fixed preview even when a new release directory was selected. The actual selected build-CuiH6W directory EXE passed Korean CLI/TUI pipe commands, shared SQLite tasks and explicit exits (1/1, 8.6seconds). This artifact remains source6cb1b9b and does not contain later resize fixes. README now describes unique package output, explicit test selection and source-matched release receipts accurately; no newer compiled acceptance is implied.

## Subsequent source corrections before the next package

Production portable parent-console handling passed the actual NSIS/Electron matrix and redirect/fault regression using the production transformation, with independent review. Native resize detection and pending readline input restoration are also in source. Existing released EXEs still predate these corrections until a new source-matched build completes; see packaged-terminal-focused-review.md.

Output/body metadata inspection and per-owner corruption isolation passed root90 tests. The current event publisher now commits related metadata and its durable event before ordered listener notification; root15 focused/Core/client and32 recovery/property regressions passed. Store/service task-scoped SQL pagination now avoids decoding unrelated Tasks, keeps30-row ID ordering and authenticates cursors per Task/mode/connection. A duplicate JSON ownership parser mismatch was reproduced and corrected before packaging; root4 targeted tests and author41 regression tests passed. Exact reviews and limitations are retained in body-pages-focused-review.md, run-pagination-focused-review.md and output-publication-contract.md.

These source corrections do not complete wire v2 or the output vertical slice. Actual fullRun and existing GUI/CLI/TUI still materialize output. Atomic staged output/index/version production, bounded Markdown presentation, complete-version copy/save/capture and remaining capacity/security/provider/GUI acceptance are still required. Code Generation remains IN_PROGRESS.

## Rebuilt release with verified console corrections

release/build-SOpzFF completed at2026-09-08T20:30:44.947Z from source6d63b50460da6b002d6df32e22fba5d10a2cac61/treea9e88d9dc2c39c074416b9e8c7f63abb07cf5fbf. Portable EXE118813172bytes has SHA-256 b911918d80606a290c2b24afb3bc5cf5df2f931bd7ca3d1c12e5d410d5ebe5c2; source ZIP SHA-2565652754ce8a5ea3a7e62d201bdc0f05f330c8701991093b4ce905552a6b13d4d. Root independently verified both artifact hashes and decompressed all211 source files against actual Git blob identities. Package verification checked16208 ASAR files and frozen native inputs. Old fixed preview hashes remain unchanged.

Actual new directory GUI/save and presenter persistence/Busy passed2/2; actual directory+portable Korean ConPTY/resize/input/exit passed2/2. Actual renamed portable GUI startup failed exit1 before DevTools and remains open. See packaged-terminal-focused-review.md for the preserved failure and diagnostic boundary. A completed artifact receipt is not overall product acceptance.

The preserved portable GUI failure was followed by a successful same-Electron GUI-only direct/wrapper control and an unchanged full-product retry after637251756bytes of verified fixture duplicates were reclaimed. The actual renamed portable GUI test then passed1/1 in40.00seconds on the identical EXE, including original-folder selection and approved sample save. This supports extraction-space pressure as an explanation; no direct ENOSPC evidence or low-space gate is claimed. Detailed hashes, fixture identity and cleanup receipt are in packaged-terminal-focused-review.md. No production change was made for this diagnostic.
